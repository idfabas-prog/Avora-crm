import { RoutePlaceholder } from "@/components/RoutePlaceholder";

export default function StaffPage() {
  return <RoutePlaceholder description="Staff roles, permissions, and location membership will be managed here." title="Staff" />;
}
