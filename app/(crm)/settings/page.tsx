import { RoutePlaceholder } from "@/components/RoutePlaceholder";

export default function SettingsPage() {
  return <RoutePlaceholder description="Organization, location, and CRM configuration will be managed here." title="Settings" />;
}
